##  Chapter 5 Exercises

1. assert spam < 10

2. assert eggs.lower() != bacon.lower()

3. assert False

4.
 ```python
import random
logging and logging.basicConfig(level=logging.DEBUG, format= '%(asctime)s - %(levelname)s - %(message)s')
```

5.
```python
import random
logging.basicConfig(filename='myProgramLog.txt', level=logging.DEBUG, format=' %(asctime)s -  %(levelname)s -  %(message)s')
```

6. 
* Debug
* Info
* Warning
* Error
* Critical

7. logging.disable()

8. With print(), all you see is the text. With logging, you instantly know it's an error, not just another random message

